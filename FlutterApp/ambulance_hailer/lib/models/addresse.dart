class Address {
  String placeFormattedAdr;
  String placeName;
  String placeId;
  double latitude;
  double longitude;

  Address(this.placeFormattedAdr, this.placeName, this.placeId, this.latitude,
      this.longitude);

}

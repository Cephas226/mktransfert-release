/*
import 'package:flutter/material.dart';
import 'package:mktransfert/src/page/beneficiaire.dart';
import 'package:mktransfert/src/page/items/beneficiaireItems.dart';


class BeneficiaireList extends StatelessWidget {
  final List<Beneficiaire> items;

  BeneficiaireList({Key key, this.items});

  @override
  Widget build(BuildContext context) {
    return ListView.builder(
      itemCount: items.length,
      itemBuilder: (context, index) {
        return BeneficiaireItem(item: items[index]);
      },
    );
  }
}
*/
